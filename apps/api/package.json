{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest run"
  },
  "dependencies": {
    "@pawnote/database": "workspace:*",
    "@pawnote/repositories": "workspace:*",
    "@pawnote/types": "workspace:*",
    "drizzle-orm": "catalog:",
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "@types/better-sqlite3": "catalog:",
    "better-sqlite3": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^4.4.0"
  }
}