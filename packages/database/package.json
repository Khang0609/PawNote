{
  "name": "@pawnote/database",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "echo \"No tests defined in database\"",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed:generate": "tsx src/seed.ts",
    "db:seed": "pnpm db:seed:generate && wrangler d1 execute pawnote-db --local --file=drizzle/seed.sql --config=../../apps/api/wrangler.jsonc --yes"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pawnote/types": "workspace:*",
    "dotenv": "^17.4.2",
    "drizzle-orm": "catalog:",
    "wrangler": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "drizzle-kit": "catalog:",
    "tsx": "^4.22.4"
  }
}